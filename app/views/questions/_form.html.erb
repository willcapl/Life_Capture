<div class="d-none">
  <%= simple_form_for [@sub_user, @question], data: { record_video_id: @question.id, sub_user_id: @sub_user.id } do |f| %>
    <%= f.input :title %>
    <%= f.input :video, as: :file %>
    <%= f.submit %>
  <% end  %>
</div>
<div class="container">
  <%= link_to 'back to questions', sub_user_questions_path(@sub_user) %>
  <div class="question-title-card">
    <h2><%= @question.title %></h2>
  </div>
  <div class="video-container">
    <div style="width: 100%; height: 100%; overflow:hidden">
      <div class="flip-horizontal record-video">
        <video style="border: 1px solid red" id="live" width="100%" height="100%" class="live-video-record" crop="crop" autoplay muted></video>
      </div>
    <button id="start" class="start-btn">Start</button>
    <button id="stop" class="stop-btn">Stop and save</button>
    </div>
  </div>
</div>
